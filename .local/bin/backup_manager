#!/usr/bin/env bash

dotfiles="$HOME/.config/"

dotfiles(){
	# select
	$dotfiles
}

# sync_list=(
# 	"$HOME/.config/dmenu/" 
# 	"$HOME/.config/dwm/" 
# 	"$HOME/.config/dwmblocks/"
# 	"$HOME/.config/rofi/"
# 	"$HOME/.config/tmux/"
# 	"$HOME/.config/nvim/"
# 	)

case "$(printf ".config\nfiles\nsend_all" | dmenu -i -c -l 10)" in 
	.config) dotfiles ;;
	*) exit 1 ;;
esac

# find $dotfiles | dmenu -c
