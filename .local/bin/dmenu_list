#!/usr/bin/env bash

folder="$HOME/.local/bin/"
selected() {
  choice=$(
    echo "$(find "$folder" | sort -nr | cut -d' ' -f2- | awk -F'/' '{print $6}')" \
    | dmenu -c -l 10 -i
  )
  command $choice
}

selected


